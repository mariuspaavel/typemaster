CREATE OR REPLACE FUNCTION coverpositionshift(
	aaccountid INTEGER,
	startingposition INTEGER,
	amount INTEGER
)
RETURNS void
LANGUAGE plpgsql
AS $$
BEGIN
	UPDATE covers SET position=position+amount WHERE accountid=aaccountid AND position >= startingposition;
END;
$$;
