CREATE TABLE covers(
	coverid
		SERIAL
		PRIMARY KEY,
	accountid
		INTEGER 
		REFERENCES accounts(accountid)
		NOT NULL,
	mediaid 
		INTEGER
		REFERENCES media(mediaid)
		NOT NULL,
	position INTEGER
		NOT NULL
);


