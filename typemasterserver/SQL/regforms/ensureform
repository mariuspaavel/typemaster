
CREATE OR REPLACE FUNCTION ensureform(
	asessionid INTEGER
)
RETURNS void
LANGUAGE plpgsql
AS $$
BEGIN
	IF NOT EXISTS(SELECT 1 FROM regforms WHERE sessionid=asessionid) THEN
		INSERT INTO regforms(sessionid) VALUES (asessionid); 
	END IF;
END;
$
