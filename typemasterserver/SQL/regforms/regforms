CREATE TABLE regforms (
	sessionid 
		INTEGER 
		REFERENCES sessions(sessionid),
	creationt 
		BIGINT
		DEFAULT (extract(epoch from now()) * 1000) 
		NOT NULL, 
	email 
		varchar(50)  
		UNIQUE, 
	password 
		varchar(32)
);


