
CREATE OR REPLACE FUNCTION submitcontact(
	asessionid INTEGER,
	acontact VARCHAR(50)
)
RETURNS void
LANGUAGE plpgsql
AS $$
BEGIN
	
	PERFORM ensureform(asessionid);
	UPDATE regforms SET contact=acontact WHERE sessionid=asessionid;
END;
$$;


