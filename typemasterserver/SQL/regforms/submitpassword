CREATE OR REPLACE FUNCTION submitpassword(
	asessionid INTEGER,
	apassword VARCHAR(32)
)
RETURNS void
LANGUAGE plpgsql
AS $$
BEGIN
	PERFORM ensureform(asessionid);
	UPDATE regforms SET password=apassword WHERE sessionid=asessionid;
END;
$$;
